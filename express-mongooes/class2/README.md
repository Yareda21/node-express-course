# Creating database and connection with the backend

## creating a database

-   we have two options for that
    -   Local => use mongoDB compase to create a datbase and use connection string to make the connection
    -   Cloud => go to mongoDB atlas and bring the connection string

## creating a model schema

-   create a folder called `models` inside called `Contact.js`


**Exercise:**

1. Start a simple Node.js server (e.g., using Express).
2. Create a function to connect your server to a MongoDB database (use either a local or cloud connection string).
3. Create a folder named `models` and inside it, create a file called `Student.js` to define a Mongoose schema for a student.
